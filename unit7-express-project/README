# Express Video API

> 慕课网584 Node.js 工程师养成计划：基于 `Express + MongoDB + Mongoose + Redis` 的视频接口练习项目。

![Node.js](https://img.shields.io/badge/Node.js-runtime-339933?style=flat-square&logo=node.js&logoColor=white)
![Express](https://img.shields.io/badge/Express-4.x-000000?style=flat-square&logo=express&logoColor=white)
![MongoDB](https://img.shields.io/badge/MongoDB-Mongoose-47A248?style=flat-square&logo=mongodb&logoColor=white)
![License](https://img.shields.io/badge/license-ISC-blue?style=flat-square)

## 项目概览

`unit7-express-project` 是一个 RESTful 风格的视频服务端 Demo，接口统一挂载在 `/api/v1` 下，默认端口为 `8888`。

当前已覆盖用户注册登录、资料编辑、头像上传、频道关注、视频发布、视频列表、视频详情、评论、点赞、点踩、喜欢列表、视频收藏和 Redis 热门视频等核心能力。

```text
Base URL: http://localhost:8888/api/v1
Static:   http://localhost:8888/<public-file-name>
```

## 技术栈

| 分类       | 技术                  |
| ---------- | --------------------- |
| Web 框架   | `express`             |
| 数据库     | `mongodb`、`mongoose` |
| 缓存/排行  | `redis`、`ioredis`    |
| 鉴权       | `jsonwebtoken`        |
| 参数校验   | `express-validator`   |
| 文件上传   | `multer`              |
| 跨域与日志 | `cors`、`morgan`      |
| 开发工具   | `nodemon`、`prettier` |

## 快速启动

### 1. 安装依赖

```bash
npm install
```

### 2. 启动 MongoDB 和 Redis

项目默认连接本地 MongoDB：

```js
module.exports.mongopath = 'mongodb://localhost:27017/express-video'
```

配置位置：

```text
config/config.default.js
```

热门视频功能依赖本地 Redis，默认连接：

```text
127.0.0.1:6379
```

可以先测试 Redis 是否正常：

```bash
redis-cli ping
```

返回 `PONG` 说明 Redis 可用。

### 3. 启动服务

```bash
npm run dev
```

启动成功后访问：

```text
http://localhost:8888
```

## 目录结构

```text
unit7-express-project
├── app.js                       # 项目入口
├── app-1.js                     # Express 练习入口
├── app-2.js                     # Express 练习入口
├── app-3.js                     # Express 练习入口
├── app-4.js                     # Express 练习入口
├── config/
│   └── config.default.js        # MongoDB 与 JWT 配置
├── controller/
│   ├── userController.js        # 用户与频道控制器
│   └── videoController.js       # 视频、评论、点赞控制器
├── middleware/
│   └── validator/               # 参数校验中间件
├── model/
│   ├── baseModel.js             # 公共时间字段
│   ├── userModel.js             # 用户模型
│   ├── videoModel.js            # 视频模型
│   ├── subscribeModel.js        # 关注模型
│   ├── videocommentModel.js     # 评论模型
│   ├── videolikeModel.js        # 喜欢/不喜欢模型
│   ├── collectModel.js          # 收藏模型
│   ├── redis/                   # Redis 连接与热门视频热度同步
│   └── index.js                 # Mongoose 连接与模型导出
├── public/                      # 静态资源与上传文件
├── router/
│   ├── index.js                 # 路由总入口
│   ├── user.js                  # 用户路由
│   └── video.js                 # 视频路由
└── util/
    ├── jwt.js                   # Token 生成与校验
    └── md5.js                   # 密码加密
```

## 鉴权方式

登录成功后接口会返回 `token`。需要登录的接口请在请求头中携带：

```http
Authorization: Bearer <token>
```

项目中的 `verifyToken(false)` 表示该接口允许游客访问；如果请求带了合法 token，响应会额外包含当前用户相关状态。

## 接口总览

### 用户与频道

| 功能         | Method   | Path                         | Auth |
| ------------ | -------- | ---------------------------- | ---- |
| 用户注册     | `POST`   | `/user/register`             | 否   |
| 用户登录     | `POST`   | `/user/logins`               | 否   |
| 获取用户列表 | `GET`    | `/user/list`                 | 是   |
| 编辑当前用户 | `PUT`    | `/user/`                     | 是   |
| 删除当前用户 | `DELETE` | `/user/`                     | 否   |
| 上传头像     | `POST`   | `/user/headimg`              | 是   |
| 关注频道     | `GET`    | `/user/subscribe/:userId`    | 是   |
| 取消关注频道 | `GET`    | `/user/unsubscribe/:userId`  | 是   |
| 获取频道信息 | `GET`    | `/user/getuser/:userId`      | 可选 |
| 获取关注列表 | `GET`    | `/user/getsubscribe/:userId` | 否   |
| 获取粉丝列表 | `GET`    | `/user/getchannel`           | 是   |

### 视频与互动

| 功能         | Method   | Path                                 | Auth |
| ------------ | -------- | ------------------------------------ | ---- |
| 获取视频列表 | `GET`    | `/video/videolist`                   | 否   |
| 获取视频详情 | `GET`    | `/video/:videoId`                    | 可选 |
| 上传视频     | `POST`   | `/video/createVideo`                 | 是   |
| 删除视频     | `DELETE` | `/video/:videoId`                    | 是   |
| 添加评论     | `POST`   | `/video/comment/:videoId`            | 是   |
| 获取评论列表 | `GET`    | `/video/commentlist/:videoId`        | 否   |
| 删除评论     | `DELETE` | `/video/comment/:videoId/:commentId` | 是   |
| 喜欢视频     | `GET`    | `/video/like/:videoId`               | 是   |
| 不喜欢视频   | `GET`    | `/video/dislike/:videoId`            | 是   |
| 获取喜欢列表 | `GET`    | `/video/likelist`                    | 是   |
| 收藏视频     | `GET`    | `/video/collect/:videoId`            | 是   |
| 获取热门视频 | `GET`    | `/video/gethots/:topnum`             | 否   |

## 请求示例

### 用户注册

```http
POST /api/v1/user/register
Content-Type: application/json
```

```json
{
  "username": "tom",
  "password": "123456",
  "email": "tom@test.com",
  "age": 18,
  "phone": "13800138000"
}
```

校验规则：

| 字段       | 规则                           |
| ---------- | ------------------------------ |
| `username` | 不能为空                       |
| `password` | 不能为空                       |
| `email`    | 必填，格式合法，不能重复       |
| `phone`    | 必填，手机号格式合法，不能重复 |
| `age`      | 必填，长度 1-3                 |

### 用户登录

```http
POST /api/v1/user/logins
Content-Type: application/json
```

```json
{
  "email": "tom@test.com",
  "password": "123456"
}
```

成功后返回用户信息和 `token`。

### 编辑当前用户

```http
PUT /api/v1/user/
Authorization: Bearer <token>
Content-Type: application/json
```

```json
{
  "username": "tom-updated",
  "image": "avatar.png",
  "cover": "cover.png",
  "channeldes": "my channel"
}
```

### 上传头像

```http
POST /api/v1/user/headimg
Authorization: Bearer <token>
Content-Type: multipart/form-data
```

表单字段：

| 字段      | 类型   | 说明     |
| --------- | ------ | -------- |
| `headimg` | `File` | 头像文件 |

成功后返回：

```json
{
  "filepath": "xxxxx.jpg"
}
```

文件会保存到 `public/` 目录，并通过 `express.static('public')` 对外提供访问。

### 上传视频

```http
POST /api/v1/video/createVideo
Authorization: Bearer <token>
Content-Type: application/json
```

```json
{
  "title": "node express demo",
  "description": "first video",
  "cover": "cover.png"
}
```

校验规则：

| 字段    | 规则                  |
| ------- | --------------------- |
| `title` | 必填，长度不能超过 20 |

### 添加评论

```http
POST /api/v1/video/comment/:videoId
Authorization: Bearer <token>
Content-Type: application/json
```

```json
{
  "content": "写得不错"
}
```

校验规则：

| 字段      | 规则                  |
| --------- | --------------------- |
| `content` | 必填，长度不能超过 50 |

### 收藏视频

```http
GET /api/v1/video/collect/:videoId
Authorization: Bearer <token>
```

同一个用户不能重复收藏同一个视频。收藏成功后会把该视频的 Redis 热度增加 `3`。

### 获取热门视频

```http
GET /api/v1/video/gethots/:topnum
```

`:topnum` 表示返回前多少个热门视频。例如：

```http
GET /api/v1/video/gethots/10
```

响应中的 `hotslist` 是一个对象，key 是 `videoId`，value 是热度分数：

```json
{
  "hotslist": {
    "69e872133d91f5e186dc5984": "8"
  }
}
```

## 分页参数

以下接口支持分页：

| 接口                              | 参数                  | 默认值    |
| --------------------------------- | --------------------- | --------- |
| `GET /video/videolist`            | `pageNum`、`pageSize` | `1`、`10` |
| `GET /video/commentlist/:videoId` | `pageNum`、`pageSize` | `1`、`10` |
| `GET /video/likelist`             | `pageNum`、`pageSize` | `1`、`10` |

分页响应通常包含：

```json
{
  "pageNum": 1,
  "pageSize": 10,
  "total": 100,
  "totalPages": 10
}
```

## 响应状态

| 状态码 | 说明                         |
| ------ | ---------------------------- |
| `200`  | 请求成功                     |
| `201`  | 创建成功                     |
| `400`  | 参数格式错误                 |
| `401`  | 业务校验失败或参数校验失败   |
| `402`  | Token 缺失、无效，或登录失败 |
| `403`  | 无权限                       |
| `404`  | 资源不存在                   |
| `422`  | Mongoose 数据校验失败        |
| `500`  | 服务端错误                   |

## 数据模型

| Model          | 主要字段                                                                                          | 说明                          |
| -------------- | ------------------------------------------------------------------------------------------------- | ----------------------------- |
| `User`         | `username`、`password`、`email`、`age`、`phone`、`image`、`cover`、`channeldes`、`subscribeCount` | 用户与频道资料                |
| `Video`        | `title`、`description`、`user`、`cover`、`commentCount`、`likeCount`、`dislikeCount`              | 视频内容                      |
| `Subscribe`    | `user`、`channel`                                                                                 | 关注关系                      |
| `Videocomment` | `content`、`video`、`user`                                                                        | 视频评论                      |
| `Videolike`    | `user`、`video`、`like`                                                                           | `1` 表示喜欢，`-1` 表示不喜欢 |
| `CollectModel` | `user`、`video`                                                                                   | 用户收藏的视频                |

所有模型都包含 `baseModel` 中的公共时间字段。

## Redis 热门视频

热门视频使用 Redis 有序集合存储：

```text
key: videohots
member: videoId
score: 热度分数
```

当前热度规则：

| 行为     | 热度变化 |
| -------- | -------- |
| 观看视频 | `+1`     |
| 点赞视频 | `+2`     |
| 评论视频 | `+2`     |
| 收藏视频 | `+3`     |

相关代码：

```text
model/redis/index.js
model/redis/redishotsync.js
```

## 开发脚本

| 命令               | 说明                     |
| ------------------ | ------------------------ |
| `npm run dev`      | 启动主服务 `app.js`      |
| `npm run dev:app1` | 启动练习入口 `app-1.js`  |
| `npm run dev:app2` | 启动练习入口 `app-2.js`  |
| `npm run dev:app3` | 启动练习入口 `app-3.js`  |
| `npm run dev:app4` | 启动练习入口 `app-4.js`  |
| `npm run format`   | 使用 Prettier 格式化项目 |

## 已完成能力

| 模块 | 能力                                         |
| ---- | -------------------------------------------- |
| 用户 | 注册、登录、列表、资料编辑、头像上传         |
| 频道 | 关注、取消关注、频道信息、关注列表、粉丝列表 |
| 视频 | 发布、列表、详情、删除                       |
| 评论 | 添加评论、评论列表、删除评论                 |
| 互动 | 喜欢、不喜欢、喜欢视频列表、收藏视频         |
| 推荐 | Redis 热门视频热度同步、热门视频列表         |

## 待完善

- `DELETE /api/v1/user/` 当前仍是占位接口，后续需要补充真正的删除逻辑。
- 部分接口错误响应格式尚未完全统一，可以抽成统一错误处理中间件。
- `config.default.js` 中的数据库地址和 JWT 密钥目前是固定配置，后续可改为环境变量。
- Redis 连接地址目前使用 `ioredis` 默认本机配置，后续可改为环境变量。
- 登录接口当前路由为 `/user/logins`，如果对外发布 API，可考虑统一命名为 `/user/login`。
